syntax = "proto3";
package kitty_chan;

service CronService{
  rpc cronCreate (ICronCreate) returns (NoResponse);
}

/**Common**/
message NoResponse{}

message ICronCreate{
  string id = 1;
  string expression = 2;
}

